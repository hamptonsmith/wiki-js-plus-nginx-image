port: $OTHER_PORT
db:
    type: postgres
    host: $DB_HOST
    port: $DB_PORT
    user: $DB_USER
    pass: $DB_PASS
    db: $DB_NAME
    ssl: true
    sslOptions:
        auto: false
        rejectUnauthorized: false
